services:
    - type: web
      name: ruby
      env: node
      plan: free
      buildCommand: yarn && yarn build
      startCommand: yarn start
      envVars:
          - key: CPUS
            value: 0
